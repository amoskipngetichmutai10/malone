{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "true",
    "build": "npx pnpm -r --filter \"./artifacts/kentainers-africa\" --if-present run build",
    "typecheck": "npx pnpm -r --filter \"./artifacts/kentainers-africa\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
